name: CI

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Run lint, build, and tests
        run: ./scripts/check.sh
